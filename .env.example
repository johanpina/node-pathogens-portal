# Database
DATABASE_URL="postgresql://patogens:patogens@localhost:5432/patogens"
POSTGRES_USER=patogens
POSTGRES_PASSWORD=patogens
POSTGRES_DB=patogens

# Auth
JWT_SECRET="change-me-in-production-use-long-random-string"

# App
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Pathogen surveillance — AI weekly draft generation (Phase 2, optional)
# Anthropic API key for the "Generate with AI" draft generator (Claude + web search).
# Recommended in production; can also be stored in the Setting `anthropic_api_key` (dev only).
ANTHROPIC_API_KEY=""
# Shared secret for the external weekly cron to POST /api/surveillance/generate
# via the `x-cron-secret` header (instead of an admin session).
CRON_SECRET=""
